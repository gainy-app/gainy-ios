query FetchStockMedianMonth($symbol: String!, $date: timestamp!) {
    tickers(where: {symbol: {_eq: $symbol}}) {
        ticker_industries {
            industry_order            
        }
    }
}

