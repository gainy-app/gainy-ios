query GetTTFTags($uniqID: String!, $profileId: Int!) {
collection_match_score_explanation(where: {profile_id: {_eq: $profileId}, collection_uniq_id: {_eq: $uniqID}}) {
    interest {
          id
          name
          icon_url
        }
        category {
          id
          name
          icon_url
          collection_id
        }
    }
}
