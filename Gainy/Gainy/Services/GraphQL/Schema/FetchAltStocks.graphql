query FetchAltStocks($symbol: String!) {
  ticker_interests(where: {symbol: {_eq: $symbol}}) {
    symbol
    interest_id
    interest {
      ticker_interests {
        ticker {
          ...RemoteTickerDetails
        }
      }
    }
  }
}
