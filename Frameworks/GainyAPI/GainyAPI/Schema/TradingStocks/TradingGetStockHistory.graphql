query TradingGetStockHistory($profile_id: Int!, $symbol: String!) {
 app_trading_orders(where: {profile_id: {_eq: $profile_id}, symbol: {_eq: $symbol}}, order_by: {created_at: desc}) {
  id
  created_at
  target_amount_delta
  history{
   ...TradingHistoryFrag
  }
 }
}
