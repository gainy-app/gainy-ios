query KycSuggestAddresses(
  $query: String!
  $limit: Int!
) {
  kyc_suggest_addresses(
    query: $query
    limit: $limit
  ){ 
    formatted_address
    street1
    street2
    city
    province
    postal_code
    country
  }
}
